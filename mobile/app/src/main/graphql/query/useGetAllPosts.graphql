query useGetAllPosts {
    posts(order_by: [{created_at: desc}]) {
        id
        name_food
        seen
        address
        picture
        user {
            id
            name
        }
    }
}
